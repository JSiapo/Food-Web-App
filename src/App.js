import React from 'react';
import AppBarContainer from 'components/appBar';

const App = () => {
  return (
    <React.Fragment>
      <AppBarContainer usuario={'José'} />
    </React.Fragment>
  );
};

export default App;
