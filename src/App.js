import React from 'react';
import AppBarContainer from 'components/appBar';

const App = () => {
  return (
    <React.Fragment>
      <AppBarContainer username={'José'} />
    </React.Fragment>
  );
};

export default App;
